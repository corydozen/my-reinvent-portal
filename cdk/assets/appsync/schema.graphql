type Query {
  getMe: User
}

type Mutation {
  savePassword(awsPassword: String): User
}

schema {
  query: Query
  mutation: Mutation
}

type User {
  PK: String
  SK: String
  email: String
  awsPassword: String
}
